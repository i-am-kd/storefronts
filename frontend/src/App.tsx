import './App.css'

// frontend/src/App.tsx (TEMPORARY TEST)
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-blue-50">
      <div className="text-center p-8 bg-white rounded-lg shadow">
        <h1 className="text-2xl font-bold text-blue-900 mb-2">✅ Storefronts Frontend Loaded</h1>
        <p className="text-gray-600">React + TypeScript + Tailwind + Vite is working correctly.</p>
        <p className="mt-4 text-sm text-gray-400">Restore App.tsx after testing.</p>
      </div>
    </div>
  );
}